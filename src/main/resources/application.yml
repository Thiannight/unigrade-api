spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

poja:
  security:
    jwt:
      secret: ${POJA_JWT_SECRET:CHANGE-ME-local-dev-only-secret-please-32-bytes-min}
      expiration-minutes: ${POJA_JWT_EXPIRATION_MINUTES:60}
